package selector

const (
	CSS             = "css selector"
	XPath           = "xpath"
	LinkText        = "link text"
	PartialLinkText = "partial link text"

	// TODO: Maybe can improved to just `Tag`.
	TagName = "tag name"
)
